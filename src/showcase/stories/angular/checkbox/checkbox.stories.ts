import { SbbCheckbox } from '@sbb-esta/lyne-angular/checkbox';
import type { Args, Meta } from '@storybook/angular';
import { argsToTemplate, moduleMetadata } from '@storybook/angular';
import { withActions } from 'storybook/actions/decorator';

const meta: Meta = {
  decorators: [
    withActions,
    moduleMetadata({
      // add slotted components or remove
      imports: [],
    }),
  ],
  title: 'elements/sbb-checkbox',
  component: SbbCheckbox,
  parameters: {
    // add events or remove the 'action' object
    actions: { handles: ['click'] },
  },
  render: (args: Args) => ({
    props: { ...args },
    template: `<sbb-checkbox ${argsToTemplate(args)}></sbb-checkbox>`,
  }),
};
export default meta;

export const Default = {};
